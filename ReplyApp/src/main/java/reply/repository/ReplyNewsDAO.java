package reply.repository;

import reply.domain.ReplyNews;
import reply.mybatis.ReplyMybatisConfig;

public class ReplyNewsDAO {
	ReplyMybatisConfig config = ReplyMybatisConfig.getInstance();
	
	public int insert(ReplyNews replyNews) {
		int result = 0;
		
		
		return result;
		
	}
}
